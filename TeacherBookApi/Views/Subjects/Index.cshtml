@model IEnumerable<TeacherBookApi.Models.Subject>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameSubject)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameSubject)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSubject">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdSubject">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSubject">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
